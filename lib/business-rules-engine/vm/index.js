"use strict";

const VmFactory = require('./VmFactory');

module.exports = {
    /**
     * Factory for creating virtual machines.
     * @type {VmFactory}
     * @memberof module:business-rules-engine
     * @see VmFactory
     */
    VmFactory
};
